# MAME Golden Clip Inventory

Last reviewed: `2026-05-29`

This inventory records local MAME red-label media windows used to drive the
clean rewrite. Generated MP4/WAV/screensheet artifacts stay under ignored
`target/reference-media/` paths and must not be committed.

## Local Artifacts

- 60 second attract/scoring capture:
  `target/reference-media/mame/defender-red-label-golden-60s.mp4`
- 60 second attract/scoring audio:
  `target/reference-media/mame/defender-red-label-golden-60s.wav`
- 1 fps contact sheet:
  `target/reference-media/inventory/defender-red-label-golden-60s/contact-sheet-1fps.png`
- 45-60 second contact sheet:
  `target/reference-media/inventory/defender-red-label-golden-60s/contact-sheet-45-60-4fps.png`
- Scripted fire/reverse MAME smoke:
  `target/reference-media/mame/scripted-fire-reverse-smoke.mp4`
- Scripted fire/reverse clean smoke:
  `target/reference-media/clean/scripted-fire-reverse-smoke.gif`
- Scripted smoke comparison report:
  `target/reference-media/scripted-fire-reverse-smoke-make-check/report.json`
- Timestamp-aligned scoring laser/explosion report:
  `target/reference-media/scoring-laser-explosion-check/report.json`
- Scoring laser/explosion contact sheets:
  `target/reference-media/explosion-materialize-crops/mame-contact.png` and
  `target/reference-media/explosion-materialize-crops/clean-contact.png`
- Scoring explosion/materialization close crops:
  `target/reference-media/explosion-materialize-crops/mame-frame008-explosion-crop.png`
  and
  `target/reference-media/explosion-materialize-crops/clean-frame008-explosion-crop.png`
- Delayed-start live fire/reverse MAME capture:
  `target/reference-media/mame/gameplay-fire-reverse-delayed-start.mp4`
- Delayed-start live fire/reverse MAME audio:
  `target/reference-media/mame/gameplay-fire-reverse-delayed-start.wav`
- Delayed-start live fire/reverse clean candidate:
  `target/reference-media/clean/gameplay-fire-reverse-delayed-start.gif`
- Delayed-start live fire/reverse clean audio:
  `target/reference-media/clean/gameplay-fire-reverse-delayed-start.wav`
- Delayed-start live fire/reverse clean sound-event timeline:
  `target/reference-media/clean/gameplay-fire-reverse-delayed-start.events.tsv`
- Delayed-start live fire/reverse report:
  `target/reference-media/gameplay-fire-reverse-delayed-check/report.json`
- Delayed-start live fire/reverse current bounded report:
  `target/reference-media/gameplay-fire-reverse-delayed-check-current/report.json`
- Delayed-start live fire/reverse player-orientation crop evidence:
  `target/reference-media/reverse-crops/mame-player-crop.png` and
  `target/reference-media/reverse-crops/clean-player-crop-current.png`
- Delayed-start thrust MAME capture:
  `target/reference-media/mame/gameplay-thrust-delayed-start.mp4`
- Delayed-start thrust MAME audio:
  `target/reference-media/mame/gameplay-thrust-delayed-start.wav`
- Delayed-start thrust MAME trace:
  `target/reference-media/mame/traces/gameplay-thrust-delayed-start.expected.tsv`
- Delayed-start thrust clean candidate:
  `target/reference-media/clean/gameplay-thrust-delayed-start.gif`
- Delayed-start thrust clean audio:
  `target/reference-media/clean/gameplay-thrust-delayed-start.wav`
- Delayed-start thrust clean sound-event timeline:
  `target/reference-media/clean/gameplay-thrust-delayed-start.events.tsv`
- Delayed-start thrust report:
  `target/reference-media/gameplay-thrust-delayed-check/report.json`
- Delayed-start thrust/reverse MAME capture:
  `target/reference-media/mame/gameplay-thrust-reverse-delayed-start.mp4`
- Delayed-start thrust/reverse MAME audio:
  `target/reference-media/mame/gameplay-thrust-reverse-delayed-start.wav`
- Delayed-start thrust/reverse MAME trace:
  `target/reference-media/mame/traces/gameplay-thrust-reverse-delayed-start.expected.tsv`
- Delayed-start thrust/reverse MAME sound-DAC trace:
  `target/reference-media/mame/traces/gameplay-thrust-reverse-delayed-start.sound-dac.tsv`
- Delayed-start thrust/reverse clean candidate:
  `target/reference-media/clean/gameplay-thrust-reverse-delayed-start.gif`
- Delayed-start thrust/reverse clean audio:
  `target/reference-media/clean/gameplay-thrust-reverse-delayed-start.wav`
- Delayed-start thrust/reverse clean sound-event timeline:
  `target/reference-media/clean/gameplay-thrust-reverse-delayed-start.events.tsv`
- Delayed-start thrust/reverse clean debug timeline:
  `target/reference-media/clean/gameplay-thrust-reverse-delayed-start.debug.tsv`
- Delayed-start thrust/reverse report:
  `target/reference-media/gameplay-thrust-reverse-delayed-check/report.json`
- Delayed-start smart-bomb MAME capture:
  `target/reference-media/mame/gameplay-smart-bomb-delayed-start.mp4`
- Delayed-start smart-bomb MAME audio:
  `target/reference-media/mame/gameplay-smart-bomb-delayed-start.wav`
- Delayed-start smart-bomb MAME trace:
  `target/reference-media/mame/traces/gameplay-smart-bomb-delayed-start.expected.tsv`
- Delayed-start smart-bomb MAME debug trace:
  `target/reference-media/mame/traces/gameplay-smart-bomb-delayed-start.debug.tsv`
- Delayed-start smart-bomb clean candidate:
  `target/reference-media/clean/gameplay-smart-bomb-delayed-start.gif`
- Delayed-start smart-bomb clean audio:
  `target/reference-media/clean/gameplay-smart-bomb-delayed-start.wav`
- Delayed-start smart-bomb clean sound-event timeline:
  `target/reference-media/clean/gameplay-smart-bomb-delayed-start.events.tsv`
- Delayed-start smart-bomb report:
  `target/reference-media/gameplay-smart-bomb-delayed-check/report.json`
- Gameplay hyperspace-death MAME capture:
  `target/reference-media/mame/gameplay-hyperspace-death-probe.mp4`
- Gameplay hyperspace-death MAME audio:
  `target/reference-media/mame/gameplay-hyperspace-death-probe.wav`
- Gameplay hyperspace-death MAME trace:
  `target/reference-media/mame/traces/gameplay-hyperspace-death-probe.expected.tsv`
- Gameplay hyperspace-death MAME debug trace:
  `target/reference-media/mame/traces/gameplay-hyperspace-death-probe.debug.tsv`
- Gameplay hyperspace-death clean candidate:
  `target/reference-media/clean/gameplay-hyperspace-death-probe.gif`
- Gameplay hyperspace-death clean audio:
  `target/reference-media/clean/gameplay-hyperspace-death-probe.wav`
- Gameplay hyperspace-death clean sound-event timeline:
  `target/reference-media/clean/gameplay-hyperspace-death-probe.events.tsv`
- Gameplay hyperspace-death report:
  `target/reference-media/gameplay-hyperspace-death-probe-check/report.json`
- Gameplay laser-hit MAME capture:
  `target/reference-media/mame/gameplay-laser-hit-single.mp4`
- Gameplay laser-hit MAME audio:
  `target/reference-media/mame/gameplay-laser-hit-single.wav`
- Gameplay laser-hit MAME trace:
  `target/reference-media/mame/traces/gameplay-laser-hit-single.expected.tsv`
- Gameplay laser-hit MAME debug trace:
  `target/reference-media/mame/traces/gameplay-laser-hit-single.debug.tsv`
- Gameplay laser-hit expanded-object diagnostic trace:
  `target/reference-media/mame/traces/gameplay-laser-hit-single-expanded.debug.tsv`
- Gameplay laser-hit clean candidate:
  `target/reference-media/clean/gameplay-laser-hit-single.gif`
- Gameplay laser-hit clean audio:
  `target/reference-media/clean/gameplay-laser-hit-single.wav`
- Gameplay laser-hit clean sound-event timeline:
  `target/reference-media/clean/gameplay-laser-hit-single.events.tsv`
- Gameplay laser-hit clean debug timeline:
  `target/reference-media/clean/gameplay-laser-hit-single.debug.tsv`
- Gameplay laser-hit report:
  `target/reference-media/gameplay-laser-hit-single-check/report.json`
- Gameplay laser-hit bounded-window report:
  `target/reference-media/gameplay-laser-hit-single-check-window/report.json`
- Gameplay laser-hit materialization/coalescence all-axis report:
  `target/reference-media/gameplay-laser-hit-single-materialize-check/report.json`
- Down030 post-death laser MAME capture/audio/traces:
  `target/reference-media/mame/laser-hit-down030-fire2437.mp4`,
  `target/reference-media/mame/laser-hit-down030-fire2437.wav`, and
  `target/reference-media/mame/traces/laser-hit-down030-fire2437.*.tsv`
- Down030 post-death laser clean candidate/audio/events/debug:
  `target/reference-media/clean/laser-hit-down030-fire2437.gif`,
  `target/reference-media/clean/laser-hit-down030-fire2437.wav`,
  `target/reference-media/clean/laser-hit-down030-fire2437.events.tsv`, and
  `target/reference-media/clean/laser-hit-down030-fire2437.debug.tsv`
- Down030 post-death laser visual diagnostic report:
  `target/reference-media/laser-hit-down030-fire2437-visual-check-aligned/report.json`
- Down030 post-death laser all-axis accepted report:
  `target/reference-media/laser-hit-down030-fire2437-check-fixed/report.json`
- Non-lander target6 shot/explosion/materialization MAME capture:
  `target/reference-media/mame/non-lander-target6-fire2524/non-lander-target6-fire2524-media.mp4`
- Non-lander target6 shot/explosion/materialization MAME audio:
  `target/reference-media/mame/non-lander-target6-fire2524/non-lander-target6-fire2524-media.wav`
- Non-lander target6 shot/explosion/materialization MAME traces:
  `target/reference-media/mame/non-lander-target6-fire2524/traces/non-lander-target6-fire2524-media.expected.tsv`
  and
  `target/reference-media/mame/non-lander-target6-fire2524/traces/non-lander-target6-fire2524-media.debug.tsv`
- Non-lander target6 shot/explosion/materialization clean candidate:
  `target/reference-media/clean/non-lander-target6-fire2524/non-lander-target6-fire2524-clean.gif`
- Non-lander target6 shot/explosion/materialization clean audio/events/debug:
  `target/reference-media/clean/non-lander-target6-fire2524/non-lander-target6-fire2524-clean.wav`
  and
  `target/reference-media/clean/non-lander-target6-fire2524/non-lander-target6-fire2524-clean.events.tsv`
  and
  `target/reference-media/clean/non-lander-target6-fire2524/non-lander-target6-fire2524-clean.debug.tsv`
- Non-lander target6 shot/explosion/materialization clean-vs-MAME report:
  `target/reference-media/non-lander-target6-fire2524-check/report.json`
- Organic non-lander long-window MAME capture:
  `target/reference-media/mame/organic-nonlander-holdup-7000.mp4`
- Organic non-lander long-window MAME audio:
  `target/reference-media/mame/organic-nonlander-holdup-7000.wav`
- Organic non-lander long-window MAME traces:
  `target/reference-media/mame/traces/organic-nonlander-holdup-7000.expected.tsv`
  and
  `target/reference-media/mame/traces/organic-nonlander-holdup-7000.debug.tsv`
- Organic non-lander long-window clean candidate:
  `target/reference-media/clean/organic-nonlander-holdup-7000/organic-nonlander-holdup-7000-clean.gif`
- Organic non-lander long-window clean audio/events/debug:
  `target/reference-media/clean/organic-nonlander-holdup-7000/organic-nonlander-holdup-7000-clean.wav`
  and
  `target/reference-media/clean/organic-nonlander-holdup-7000/organic-nonlander-holdup-7000-clean.events.tsv`
  and
  `target/reference-media/clean/organic-nonlander-holdup-7000/organic-nonlander-holdup-7000-clean.debug.tsv`
- Organic non-lander long-window diagnostic report:
  `target/reference-media/organic-nonlander-holdup-7000-check/report.json`
- Organic non-lander hold-down MAME capture/audio/traces:
  `target/reference-media/mame/organic-nonlander-holddown-7000.*` and
  `target/reference-media/mame/traces/organic-nonlander-holddown-7000.*.tsv`
- Organic non-lander hold-down clean candidate/audio/events/debug:
  `target/reference-media/clean/organic-nonlander-holddown-7000.*`
- Organic non-lander hold-down visual report:
  `target/reference-media/organic-nonlander-holddown-7000-check/report.json`
- Non-lander sound/object reference-window scan reports:
  `target/reference-media/reference-window-scan.json` and
  `target/reference-media/reference-window-scan-organic.json`
- State-steered enemy explosion matrix MAME capture:
  `target/reference-media/mame/enemy-explosion-matrix-media/enemy-explosion-matrix-media.mp4`
- State-steered enemy explosion matrix MAME audio:
  `target/reference-media/mame/enemy-explosion-matrix-media/enemy-explosion-matrix-media.wav`
- State-steered enemy explosion matrix MAME traces:
  `target/reference-media/mame/enemy-explosion-matrix-media/traces/enemy-explosion-matrix-media.expected.tsv`
  and
  `target/reference-media/mame/enemy-explosion-matrix-media/traces/enemy-explosion-matrix-media.debug.tsv`
- State-steered enemy explosion matrix clean candidate:
  `target/reference-media/clean/enemy-explosion-matrix-media/enemy-explosion-matrix-clean.gif`
- State-steered enemy explosion matrix clean audio/events/debug:
  `target/reference-media/clean/enemy-explosion-matrix-media/enemy-explosion-matrix-clean.wav`
  and
  `target/reference-media/clean/enemy-explosion-matrix-media/enemy-explosion-matrix-clean.events.tsv`
  and
  `target/reference-media/clean/enemy-explosion-matrix-media/enemy-explosion-matrix-clean.debug.tsv`
- State-steered enemy explosion matrix visual report:
  `target/reference-media/enemy-explosion-matrix-check/report.json`
- State-steered enemy materialization matrix MAME capture/audio/traces:
  `target/reference-media/mame/enemy-materialize-matrix-media.mp4`,
  `target/reference-media/mame/enemy-materialize-matrix-media.wav`, and
  `target/reference-media/mame/traces/enemy-materialize-matrix-media.*.tsv`
- State-steered enemy materialization matrix clean candidate/audio/events/debug:
  `target/reference-media/clean/enemy-materialize-matrix-clean.gif`,
  `target/reference-media/clean/enemy-materialize-matrix-clean.wav`,
  `target/reference-media/clean/enemy-materialize-matrix-clean.events.tsv`,
  and `target/reference-media/clean/enemy-materialize-matrix-clean.debug.tsv`
- State-steered enemy materialization matrix visual report:
  `target/reference-media/enemy-materialize-matrix-check/report.json`
- Isolated non-lander sound-command MAME captures and WAVs:
  `target/reference-media/mame/nonlander-sound-command-fe.*`,
  `target/reference-media/mame/nonlander-sound-command-fa.*`,
  `target/reference-media/mame/nonlander-sound-command-f8.*`, and
  `target/reference-media/mame/nonlander-sound-command-f3.*`
- Isolated non-lander sound-command MAME DAC traces:
  `target/reference-media/mame/traces/nonlander-sound-command-fe.sound-dac.tsv`,
  `target/reference-media/mame/traces/nonlander-sound-command-fa.sound-dac.tsv`,
  `target/reference-media/mame/traces/nonlander-sound-command-f8.sound-dac.tsv`,
  and
  `target/reference-media/mame/traces/nonlander-sound-command-f3.sound-dac.tsv`
- Isolated non-lander sound-command clean candidates:
  `target/reference-media/clean/nonlander-sound-command-fe/nonlander-sound-command-fe-clean.*`,
  `target/reference-media/clean/nonlander-sound-command-fa/nonlander-sound-command-fa-clean.*`,
  `target/reference-media/clean/nonlander-sound-command-f8/nonlander-sound-command-f8-clean.*`,
  and
  `target/reference-media/clean/nonlander-sound-command-f3/nonlander-sound-command-f3-clean.*`
- Isolated non-lander sound-command reports:
  `target/reference-media/nonlander-sound-command-fe-check/report.json`,
  `target/reference-media/nonlander-sound-command-fa-check/report.json`,
  `target/reference-media/nonlander-sound-command-f8-check/report.json`,
  and
  `target/reference-media/nonlander-sound-command-f3-check/report.json`
- Gameplay laser-hit explosion crop/contact evidence:
  `target/reference-media/gameplay-laser-hit-explosion-crops/mame-contact.png`,
  `target/reference-media/gameplay-laser-hit-explosion-crops/mame-explosion-crop.png`,
  `target/reference-media/gameplay-laser-hit-explosion-crops/clean-contact-visible-delay.png`,
  and
  `target/reference-media/gameplay-laser-hit-explosion-crops/clean-explosion-crop-visible-delay.png`
- Long no-input gameplay observation MAME capture:
  `target/reference-media/mame/gameplay-observation-long-delayed-start.mp4`
- Long no-input gameplay observation MAME audio:
  `target/reference-media/mame/gameplay-observation-long-delayed-start.wav`
- Long no-input gameplay observation MAME trace:
  `target/reference-media/mame/traces/gameplay-observation-long-delayed-start.expected.tsv`
- Long no-input gameplay observation MAME debug trace:
  `target/reference-media/mame/traces/gameplay-observation-long-delayed-start.debug.tsv`
- Long no-input gameplay observation clean candidate:
  `target/reference-media/clean/gameplay-observation-long-delayed-start.gif`
- Long no-input gameplay observation clean audio:
  `target/reference-media/clean/gameplay-observation-long-delayed-start.wav`
- Long no-input gameplay observation clean sound-event timeline:
  `target/reference-media/clean/gameplay-observation-long-delayed-start.events.tsv`
- Long no-input gameplay observation frame evidence:
  `target/reference-media/long-observation-crops/mame-clean-keyframes.png`
- Gameplay materialization clean candidate:
  `target/reference-media/clean/gameplay-materialize-delayed-start.gif`
- Gameplay materialization clean audio:
  `target/reference-media/clean/gameplay-materialize-delayed-start.wav`
- Gameplay materialization frame evidence:
  `target/reference-media/materialize-crops/mame-clean-materialize-keyframes.png`
- High-score/Hall-of-Fame zeroed-CMOS MAME capture:
  `target/reference-media/mame/gameplay-high-score-entry-zero.mp4`
- High-score/Hall-of-Fame zeroed-CMOS MAME audio:
  `target/reference-media/mame/gameplay-high-score-entry-zero.wav`
- High-score/Hall-of-Fame zeroed-CMOS MAME trace:
  `target/reference-media/mame/traces/gameplay-high-score-entry-zero.expected.tsv`
- High-score/Hall-of-Fame contact/frame evidence:
  `target/reference-media/inventory/gameplay-high-score-entry-zero/contact-sheet-55s-77s.png`
  and
  `target/reference-media/inventory/gameplay-high-score-entry-zero/hof-frame-76.png`
- Lander pickup/pull MAME trace:
  `target/reference-media/mame/abduction-search/hold-up.expected.tsv`
- Lander pickup/pull MAME debug trace:
  `target/reference-media/mame/abduction-search/hold-up.debug.tsv`
- Lander pickup/pull MAME media capture:
  `target/reference-media/mame/abduction-hold-up-media/abduction-hold-up-media.mp4`
- Lander pickup/pull MAME media audio:
  `target/reference-media/mame/abduction-hold-up-media/abduction-hold-up-media.wav`
- Lander pickup/pull MAME media traces:
  `target/reference-media/mame/abduction-hold-up-media/traces/abduction-hold-up-media.expected.tsv`
  and
  `target/reference-media/mame/abduction-hold-up-media/traces/abduction-hold-up-media.debug.tsv`
- Lander pickup/pull clean candidate:
  `target/reference-media/clean/gameplay-abduction-hold-up.gif`
- Lander pickup/pull clean audio:
  `target/reference-media/clean/gameplay-abduction-hold-up.wav`
- Lander pickup/pull clean sound-event timeline:
  `target/reference-media/clean/gameplay-abduction-hold-up.events.tsv`
- Lander pickup/pull clean debug timeline:
  `target/reference-media/clean/gameplay-abduction-hold-up.debug.tsv`
- Lander pickup/pull bounded clean candidate:
  `target/reference-media/clean/abduction-hold-up-media/abduction-hold-up-clean.gif`
- Lander pickup/pull bounded clean audio/events/debug:
  `target/reference-media/clean/abduction-hold-up-media/abduction-hold-up-clean.wav`
  and
  `target/reference-media/clean/abduction-hold-up-media/abduction-hold-up-clean.events.tsv`
  and
  `target/reference-media/clean/abduction-hold-up-media/abduction-hold-up-clean.debug.tsv`
- Lander pickup/pull clean-vs-MAME report:
  `target/reference-media/abduction-hold-up-pickup-pull-check/report.json`
- State-steered falling-human catch MAME capture:
  `target/reference-media/mame/state-steered-media/afall-player-catch/afall-player-catch-media.mp4`
- State-steered falling-human catch MAME audio:
  `target/reference-media/mame/state-steered-media/afall-player-catch/afall-player-catch-media.wav`
- State-steered falling-human catch MAME traces:
  `target/reference-media/mame/state-steered-media/afall-player-catch/traces/afall-player-catch-media.expected.tsv`
  and
  `target/reference-media/mame/state-steered-media/afall-player-catch/traces/afall-player-catch-media.debug.tsv`
- State-steered falling-human catch contact sheet:
  `target/reference-media/mame/state-steered-media/contact-sheets/afall-player-catch-frames-1450-1471.png`
- State-steered falling-human safe-landing MAME capture:
  `target/reference-media/mame/state-steered-media/afall-safe-landing/afall-safe-landing-media.mp4`
- State-steered falling-human safe-landing MAME audio:
  `target/reference-media/mame/state-steered-media/afall-safe-landing/afall-safe-landing-media.wav`
- State-steered falling-human safe-landing MAME traces:
  `target/reference-media/mame/state-steered-media/afall-safe-landing/traces/afall-safe-landing-media.expected.tsv`
  and
  `target/reference-media/mame/state-steered-media/afall-safe-landing/traces/afall-safe-landing-media.debug.tsv`
- State-steered terrain-blow MAME capture:
  `target/reference-media/mame/state-steered-media/terrain-blow/terrain-blow-media.mp4`
- State-steered terrain-blow MAME audio:
  `target/reference-media/mame/state-steered-media/terrain-blow/terrain-blow-media.wav`
- State-steered terrain-blow MAME traces:
  `target/reference-media/mame/state-steered-media/terrain-blow/traces/terrain-blow-media.expected.tsv`
  and
  `target/reference-media/mame/state-steered-media/terrain-blow/traces/terrain-blow-media.debug.tsv`
- State-steered terrain-blow contact sheet:
  `target/reference-media/mame/state-steered-media/contact-sheets/terrain-blow-keyframes-1450-1587.png`
- State-steered falling-human catch clean candidate:
  `target/reference-media/clean/state-steered-media/afall-player-catch/afall-player-catch-clean.gif`
- State-steered falling-human catch clean audio/events/debug:
  `target/reference-media/clean/state-steered-media/afall-player-catch/afall-player-catch-clean.wav`
  and
  `target/reference-media/clean/state-steered-media/afall-player-catch/afall-player-catch-clean.events.tsv`
  and
  `target/reference-media/clean/state-steered-media/afall-player-catch/afall-player-catch-clean.debug.tsv`
- State-steered falling-human catch clean contact sheet:
  `target/reference-media/clean/state-steered-media/contact-sheets/afall-player-catch-clean-frames-1450-1471.png`
- State-steered falling-human catch clean-vs-MAME report:
  `target/reference-media/afall-player-catch-check/report.json`
- State-steered falling-human safe-landing clean candidate:
  `target/reference-media/clean/state-steered-media/afall-safe-landing/afall-safe-landing-clean.gif`
- State-steered falling-human safe-landing clean audio/events/debug:
  `target/reference-media/clean/state-steered-media/afall-safe-landing/afall-safe-landing-clean.wav`
  and
  `target/reference-media/clean/state-steered-media/afall-safe-landing/afall-safe-landing-clean.events.tsv`
  and
  `target/reference-media/clean/state-steered-media/afall-safe-landing/afall-safe-landing-clean.debug.tsv`
- State-steered falling-human safe-landing clean-vs-MAME report:
  `target/reference-media/afall-safe-landing-check/report.json`
- State-steered terrain-blow clean candidate:
  `target/reference-media/clean/state-steered-media/terrain-blow/terrain-blow-clean.gif`
- State-steered terrain-blow clean audio/events/debug:
  `target/reference-media/clean/state-steered-media/terrain-blow/terrain-blow-clean.wav`
  and
  `target/reference-media/clean/state-steered-media/terrain-blow/terrain-blow-clean.events.tsv`
  and
  `target/reference-media/clean/state-steered-media/terrain-blow/terrain-blow-clean.debug.tsv`
- State-steered terrain-blow clean contact sheet:
  `target/reference-media/clean/state-steered-media/contact-sheets/terrain-blow-clean-keyframes-1450-1587.png`
- State-steered terrain-blow clean-vs-MAME report:
  `target/reference-media/terrain-blow-check/report.json`

## Observed Windows

These timestamps are from the local MAME 60 second capture. They are initial
inventory anchors, not final pass criteria.

- `00.00-05.99s`: power-on/static and blue screen. AC audio energy is
  present in `00-02s`; use for boot only.
- `06.00-11.99s`: `INITIAL TESTS INDICATE UNIT OK`. No measured AC audio above
  threshold; use for boot only.
- `12.00-14.99s`: Williams logo begins appearing. AC audio energy is present
  in `12-14s`; use for title sound cue inventory.
- `15.00-28.99s`: Williams/presents/Defender title build. No measured AC audio
  above threshold; use as attract silence evidence.
- `29.00-38.99s`: Hall of Fame page. No measured AC audio above threshold; use
  for Hall of Fame visual inventory.
- `39.00-44.99s`: scoring demonstration starts with scanner and terrain. No
  measured AC audio above threshold; use for scoring scene setup.
- `45.00-51.99s`: scoring rescue/lander setup and player approach. No measured
  AC audio above threshold; use for pre-laser setup.
- `52.00-53.25s`: scoring laser and first enemy explosion/materialization
  cloud. No measured AC audio above threshold; use as the primary
  laser/explosion visual target.
- `53.25-55.00s`: `LANDER 150` score label and continued scoring scene. No
  measured AC audio above threshold; use for score-popup visual inventory.
- `55.00-56.00s`: additional laser/explosion action before point labels. No
  measured AC audio above threshold; use as a secondary visual target.
- `56.00-59.75s`: lander/mutant/baiter point labels and objects. No measured
  AC audio above threshold; use for sprite and label inventory.

## State-Steered Gameplay Windows

These clips are generated by the trace tool's state-steering mode so rare
source routines can be isolated without waiting for organic play to enter the
same object/process state. The steering is tooling-only; once the seeded state
exists, MAME executes the original red-label routines.

- `afall-player-catch`, frames `1450-1471`: a falling-human process switches
  from `AFALL` to `AFALL2` at frame `1451`. The catch sound command `0xF7`
  appears at frames `1451`, `1461`, and `1471`, and the `500` score popup is
  visible by frame `1461`.
- `afall-safe-landing`, frames `1450-1451`: the state-steered falling-human
  process reaches the safe-landing branch, raises the score to `250`, and emits
  the source `ALSND` command `0xE0` at frame `1451`.
- `terrain-blow`, frames `1450-1587`: the terrain-blow process starts from
  `TERBLO` at frame `1450`, expands through the full-screen flash and
  multicolor terrain/object pixel field, then emits the sound-command tail
  `0xEB`, `0xEE`, `0xEE`, `0xE8`, and `0xE8` at frames `1561`, `1565`,
  `1571`, `1577`, and `1587`.
- `enemy-explosion-matrix`, frames `1200-1260`: the state-steer seeds six
  source expanded-object explosion slots, then MAME advances them through
  `EXPU`. Frame `1200` reports `LNDP3`, `SCZP1`, `TIEP3`, `PRBP1`, `UFOP3`,
  and `SWXP1` at size `0x01AA`; frame `1201` advances the same slots to
  `0x0254`.
- `enemy-materialize-matrix`, frames `1200-1248`: the state-steer seeds six
  source expanded-object appearance slots backed by hidden objects so MAME's
  `APVCT` computes the same center geometry used by live coalescence. Frame
  `1200` reports `LNDP3`, `SCZP1`, `TIEP3`, `PRBP1`, `UFOP3`, and `SWPIC1` at
  size `0xAC00`; frame `1201` advances the same slots to `0xAB00`.

Clean state-steered candidates have been generated for the catch and
terrain-blow windows. The clean catch candidate uses the credited input prelude,
captures visual frames `1450-1481`, and renders candidate audio from frame 1
before trimming to the bounded comparison window. That preserves the
pre-window `0xF5`/`0xEA` audio tail plus the `0xF7` catch commands at relative
frames `1`, `11`, and `21`. The report-only media check at
`target/reference-media/afall-player-catch-check/report.json` now passes when
the reference stream starts at the MP4 PTS-aligned frame time `24111ms`: visual
RMS `29.32`, visual MAE `5.23`, envelope correlation `0.935`, RMS ratio
`1.008`, and zero-crossing ratio `0.463`.

The clean safe-landing candidate uses the credited input prelude, captures
visual frames `1450-1481`, and steers the falling human one subpixel step from
terrain so the normal clean safe-landing path emits `0xE0` at relative frame
`1`. The clean sound-board VARI path now follows the source restart loop and
uses the calibrated VARI DAC gain for the `ALSND` window. The report-only media
check at `target/reference-media/afall-safe-landing-check/report.json` now
passes when the reference stream starts on the MAME sound-command frame
`24180ms`: visual RMS `29.30`, visual MAE `5.24`, audio envelope correlation
`0.284`, RMS ratio `1.003`, and zero-crossing ratio `1.396`.

The clean terrain-blow sound-event TSV now matches the MAME relative command
offsets `1`, `4`, `9`, `13`, `17`, `21`, `26`, `32`, `38`, `44`, `52`, `61`,
`71`, `82`, `93`, `101`, `111`, `115`, `121`, `127`, and `137`. The clean
visual pass now uses source `TERX0` terrain explosion pixels, the
MAME-observed flash windows, and the MAME-observed terrain explosion growth
cadence. The report-only media check at
`target/reference-media/terrain-blow-check/report.json` now passes when the
reference stream starts at the MP4 PTS-aligned frame time `24111ms`: visual RMS
`31.19`, visual MAE `6.32`, playfield RMS `13.20`, terrain RMS `29.64`, and
audio passes through the stochastic-noise gate with envelope correlation
`0.884` and RMS ratio `1.341`.

The enemy explosion matrix is a visual-only bounded proof for expanded-object
families. The MAME/clean visual report at
`target/reference-media/enemy-explosion-matrix-check/report.json` passes with
full visual RMS `31.38`, visual MAE `5.81`, playfield RMS `15.82`, and
laser-band RMS `15.95`. The report is top-level `pass` with
`acceptance_mode=visual`; its synthetic matrix audio remains deliberately
ignored because the clip does not exercise real enemy-kill sound commands.
Audio proof for those families remains covered by organic target6/hit windows,
isolated sound-command clips, or future bounded gameplay clips.

The enemy materialization matrix is the matching visual-only bounded proof for
source appearance/coalescence families. The MAME/clean report at
`target/reference-media/enemy-materialize-matrix-check/report.json` passes with
full visual RMS `37.90`, visual MAE `7.50`, playfield RMS `35.35`, and
laser-band RMS `32.08` when the reference stream starts at `19935ms`. The clean
candidate uses the credited `first_300_frames` prelude, captures frames
`1201-1248`, and freezes live wave simulation after the state steer so the
window contains only the six enemy-family appearance clouds. The report uses
`acceptance_mode=visual`; audio remains deliberately ignored for this synthetic
appearance-only clip.

The isolated non-lander sound-command clips directly exercise the remaining
enemy-family command bytes that were not represented by a clean organic hit
window: bomber hit `0xFE`, pod hit `0xFA`, swarmer/baiter hit `0xF8`, and
swarmer shot `0xF3`. MAME captures now include per-frame sound-board DAC-write
traces (`*.sound-dac.tsv`) in addition to the command TSVs. The clean candidate
reports all pass their audio gates after calibrating the tonal GWAVE period
density for `0xFE` and `0xFA`: `0xFE` RMS ratio `1.158`, envelope correlation
`0.890`; `0xFA` RMS ratio `1.287`, envelope correlation `0.857`; `0xF8` RMS
ratio `1.202`, zero-crossing ratio `1.093`; `0xF3` RMS ratio `1.208`,
zero-crossing ratio `1.003`. The reports are top-level `pass` with
`acceptance_mode=audio`; their visual portions are not acceptance targets
because the clean and MAME windows intentionally do not share a matching
playfield.

On `2026-05-29`, the organic terrain-blow proof boundary was rechecked across
the current local rescue and terrain debug TSV inventory, including the
`rescue-terrain-cycle*`, `rescue-search*`, `rescue-afall-probe`,
`rescue-aim-search`, `rescue-catch-directed`, `rescue-catch-search`,
`abduction-search`, `abduction-hold-up-media`, and long-gameplay traces. The
search found repeatable `terrain_blown=true` rows, but they do not close the
last-human terrain-blow boundary: the early rows are wave/start transition
state, and the later organic rows still retain `ASTCNT=0x0A`. The accepted
terrain-blow evidence remains the state-steered `TERBLO` media comparison
above; an organic last-human clip still requires a targeted capture.

The `reference-window-scan` tool now records the same terrain-blow boundary in
machine-readable form. On `2026-05-29 11:36 BST`, the all-trace scan covered
`204` expected traces and `200` debug traces, found `113279`
`terrain_blown=true` rows, found `2` `TERBLO` process rows from the
state-steered terrain-blow artifacts, and found `0` last-human terrain-blow
candidates with `ASTCNT=0x00`. The organic-only scan excluding
`nonlander-sound-command`, `enemy-explosion-matrix`,
`enemy-materialize-matrix`, and `state-steered` covered `184` expected traces
and `180` debug traces, found `105596` terrain status rows, found `0`
`TERBLO` process rows, and found `0` last-human candidates.

## Current Comparison Results

### Scoring Laser And Explosion

Command used:

```sh
MAME_DIR=target/reference-media/mame
CLEAN_DIR=target/reference-media/clean
REPORT_DIR=target/reference-media/scoring-laser-explosion-check

make reference-media-check \
  REFERENCE_MEDIA="$MAME_DIR/defender-red-label-golden-60s.mp4" \
  REFERENCE_AUDIO="$MAME_DIR/defender-red-label-golden-60s.wav" \
  CANDIDATE_MEDIA="$CLEAN_DIR/clean-attract-60s.gif" \
  CANDIDATE_AUDIO="$CLEAN_DIR/clean-attract-60s.wav" \
  REFERENCE_MEDIA_REPORT_DIR="$REPORT_DIR" \
  REFERENCE_MEDIA_REFERENCE_START_MS=52000 \
  REFERENCE_MEDIA_CANDIDATE_START_MS=24000 \
  REFERENCE_MEDIA_DURATION_MS=1250 \
  REFERENCE_MEDIA_REPORT_ONLY=1
```

Result: pass. The bounded `52.00-53.25s` MAME scoring laser/explosion window
matches the clean `24.00-25.25s` scoring window within current media thresholds:
visual RMS `22.31`, visual MAE `3.07`, playfield RMS `13.57`, laser-band RMS
`1.91`, and audio passes as verified silence after DC/noise-floor handling.
The clean scoring laser now follows the MAME horizontal ship-row path instead
of interpolating diagonally to the target. The scoring
explosion/materialization effects now use source object image fragments and
source palette colors, so the lander cloud carries green/yellow alien pixels
instead of a generic monochrome placeholder cloud.

### Delayed-Start Live Fire And Reverse

Input program used:

```text
none*900;coin*4;none*360;start_one*10;none*180;fire*30;none*30;reverse*20;none*180
```

MAME trace sound-command evidence:

```text
912   0xE6 credit_added
1267  0xF5 game_started
1402  0xEA player materialize / appear
1457  0xEB laser
```

Clean event timeline after the current repair:

```text
911   CreditAdded
1265  GameStarted
1402  UnmappedSoundCommand { command: 234 }
1454  UnmappedSoundCommand { command: 235 }
```

Result: pass for the current delayed-start window. In
`target/reference-media/gameplay-fire-reverse-delayed-check-current/report.json`,
the full visual report is within current thresholds (`full` RMS `28.95`, MAE
`5.11`) with `playfield` RMS `10.80` and `laser_band` RMS `16.03`. HUD and
scanner remain the highest-error visual regions (`hud` RMS `54.31`,
`scanner` RMS `62.96`) but do not fail the current full-frame acceptance gate.
The player-orientation crop evidence shows the clean ship switching from
`PLAYER_SHIP` to `PLAYER_SHIP_LEFT` during the same reverse window as MAME.
Audio passes through the stochastic-noise gate for `0xEA` materialize and
`0xEB` laser/turbo: envelope correlation `0.931`, RMS ratio `1.020`, clean peak
`0.411` vs MAME `0.412`, and zero-crossing ratio `1.168`. The remaining audio
work is to cover the other command families in separate MAME windows, not this
laser/materialize clip.

### Delayed-Start Thrust

Input program used:

```text
none*900;coin*4;none*360;start_one*10;none*180;thrust*90;none*180
```

MAME trace sound-command evidence:

```text
912   0xE6 credit_added
1267  0xF5 game_started
1402  0xEA player materialize / appear
1457  0xE9 thrust start
1547  0xF0 thrust stop / background handoff
```

Clean event timeline after the current repair:

```text
911   CreditAdded
1265  GameStarted
1402  UnmappedSoundCommand { command: 234 }
1457  ThrustStarted
1544  ThrustStopped
```

Result: pass for the current delayed-start thrust window. In
`target/reference-media/gameplay-thrust-delayed-check/report.json`, the bounded
`24.0-26.5s` visual report passes with `full` RMS `28.44`, MAE `5.00`,
`playfield` RMS `9.85`, `laser_band` RMS `14.04`, and `terrain` RMS `18.52`.
Audio passes through the stochastic-noise gate after clean thrust start was
made to preempt the active one-shot sound, the thrust filtered-noise step was
calibrated to the MAME zero-crossing range, and thrust stop now hands off to
the source `0xF0` background-noise command with the calibrated background gain:
envelope correlation `0.916`, RMS ratio `0.772`, clean peak `0.388` vs MAME
`0.297`, and zero-crossing ratio `1.402`.

### Delayed-Start Thrust/Reverse

Input program used:

```text
none*900;coin*4;none*360;start_one*10;none*180;thrust*30;reverse,thrust*30;none*200
```

MAME trace sound-command and input evidence:

```text
912   0xE6 credit_added
1267  0xF5 game_started
1402  0xEA player materialize / appear
1457  0xE9 thrust start
1499  reverse and thrust input bits active
1517  0xF0 thrust stop / background handoff
```

Clean event timeline after the current repair:

```text
911   CreditAdded
1265  GameStarted
1402  UnmappedSoundCommand { command: 234 }
1457  ThrustStarted
1514  ThrustStopped
```

Result: pass for the delayed-start thrust/reverse window. In
`target/reference-media/gameplay-thrust-reverse-delayed-check/report.json`, the
bounded `24.0-26.5s` visual report passes with `full` RMS `28.40`, MAE `4.96`,
`playfield` RMS `9.38`, `laser_band` RMS `13.76`, and `terrain` RMS `18.81`.
Audio passes through the stochastic-noise gate with envelope correlation
`0.889`, RMS ratio `0.791`, clean peak `0.384` vs MAME `0.296`, and
zero-crossing ratio `1.621`. This clip adds accepted MAME-vs-clean media proof
for reverse-facing player orientation while thrust audio and the `0xF0`
background handoff are active.

### Delayed-Start Enemy Shot

Input program used:

```text
none*900;coin*4;none*360;start_one*10;none*180;thrust*90;none*180
```

MAME trace sound-command evidence:

```text
912   0xE6 credit_added
1267  0xF5 game_started
1402  0xEA player materialize / appear
1457  0xE9 thrust start
1547  0xF0 thrust stop / background handoff
1623  0xFC lander shot
```

Clean event timeline after the current repair:

```text
911   CreditAdded
1265  GameStarted
1402  UnmappedSoundCommand { command: 234 }
1457  ThrustStarted
1544  ThrustStopped
1622  UnmappedSoundCommand { command: 252 }
```

Result: pass for the current delayed-start enemy-shot window. In
`target/reference-media/gameplay-enemy-shot-delayed-check/report.json`, the
bounded `26.5-28.5s` visual report passes with `full` RMS `28.55`, MAE `5.01`,
`playfield` RMS `10.27`, `laser_band` RMS `15.74`, and `terrain` RMS `19.07`.
Audio passes through the stochastic-noise gate for source command `0xFC`:
`noise_like_pass=true`, envelope correlation `0.796`, RMS ratio `0.931`, clean
peak `0.690` vs MAME `0.334`, and zero-crossing ratio `1.055`.

The clean runtime now ticks the source lander shot timer on the observed
three-frame cadence, allows the initial out-of-range source fireball to emit
the `0xFC` sound event, and still culls shells at the source X bound before
they can wrap into false player hits. The sound-board GWAVE renderer also
implements the source-style delta-frequency continuation needed by the
`DP1V`/`0xFC` vector.

### Delayed-Start Smart Bomb

Input program used:

```text
none*900;coin*4;none*360;start_one*10;none*180;smart_bomb;none*180
```

MAME trace sound-command evidence:

```text
912   0xE6 credit_added
1267  0xF5 game_started
1402  0xEA player materialize / appear
1458  0xEE smart bomb
1462  0xEE smart bomb
1466  0xEE smart bomb
1470  0xEE smart bomb
1474  0xEE smart bomb
1478  0xEE smart bomb
1482  0xE8 cannon
```

Clean event timeline after the current repair:

```text
911   CreditAdded
1265  GameStarted
1402  UnmappedSoundCommand { command: 234 }
1458  UnmappedSoundCommand { command: 238 }
1462  UnmappedSoundCommand { command: 238 }
1466  UnmappedSoundCommand { command: 238 }
1470  UnmappedSoundCommand { command: 238 }
1474  UnmappedSoundCommand { command: 238 }
1478  UnmappedSoundCommand { command: 238 }
1482  UnmappedSoundCommand { command: 232 }
```

MAME active-object debug evidence shows one visible active enemy before the
smart bomb: frames `1454-1456` contain an object around `x=0x44/0x45`,
`y=0x43/0x44`, `typ=0x00`, `col=0x4433`. At frame `1457`, MAME updates the
score to `150` and smart bombs from `3` to `2`, and that visible enemy leaves
the active list. No replacement visible reserve enemy appears during the
180-frame post-bomb window. Clean now mirrors that sequence with one initial
active wave enemy, delayed playfield activation at the materialize frame,
two-phase smart-bomb scoring/flash/clear, explosion-cloud creation, delayed
reserve activation, and the `0xEE ... 0xE8` command sequence above.

Result: pass for the current delayed-start smart-bomb window. In
`target/reference-media/gameplay-smart-bomb-delayed-check/report.json`, the
full visual report is within current thresholds (`full` RMS `30.27`, MAE
`5.61`) with `playfield` RMS `10.34`, `laser_band` RMS `13.78`, and `terrain`
RMS `22.49`. Audio passes through the stochastic-noise gate:
`noise_like_pass=true`, envelope correlation `0.919`, RMS ratio `1.198`, clean
peak `0.362` vs MAME `0.332`, and zero-crossing ratio `0.490` with the current
stochastic ratio floor of `0.45`.

### Gameplay Hyperspace Death

Input program used:

```text
none*900;coin*4;none*360;start_one*10;none*180;hyperspace;none*480
```

MAME trace sound-command evidence:

```text
912   0xE6 credit_added
1267  0xF5 game_started
1402  0xEA player materialize / appear
1487  0xEA hyperspace rematerialize
1526  0xEE player death
1534  0xEE player death tail
1542  0xE8 cannon / explosion tail
1670  0xEC background/end stop command
```

Clean event timeline after the current repair:

```text
911   CreditAdded
1265  GameStarted
1402  UnmappedSoundCommand { command: 234 }
1487  UnmappedSoundCommand { command: 234 }
1526  UnmappedSoundCommand { command: 238 }
1534  UnmappedSoundCommand { command: 238 }
1542  UnmappedSoundCommand { command: 232 }
1670  UnmappedSoundCommand { command: 236 }
1773  UnmappedSoundCommand { command: 234 }
```

MAME RNG trace evidence showed the active playfield RNG advancing from
`F1/C4/19` at the first player materialize frame to `32/AD/D7` on the
hyperspace input frame, which enters the source death-risk path. Clean now
advances the source RNG during live play, hides the ship during hyperspace,
delays rematerialization to the MAME `0xEA` frame, triggers the delayed death
explosion at the MAME `0xEE` frame, and keeps the player-death sound tail alive
across respawn until the direct `0xEC` stop command. The cannon/explosion
sound-board renderer now produces a long noise tail so `0xE8` persists through
the MAME death window instead of ending after a short placeholder burst.

Result: pass for the current hyperspace-death window. In
`target/reference-media/gameplay-hyperspace-death-probe-check/report.json`,
the full visual report is within current thresholds (`full` RMS `27.99`, MAE
`4.44`) with `playfield` RMS `16.93`, `laser_band` RMS `12.08`, and `terrain`
RMS `18.60`. Audio passes through the stochastic-noise gate:
`noise_like_pass=true`, envelope correlation `0.816`, RMS ratio `1.066`, clean
peak `0.361` vs MAME `0.384`, and zero-crossing ratio `1.402`.

### Gameplay Laser Hit

Input program used:

```text
none*900;coin*4;none*360;start_one*10;none*300;fire*30;none*360
```

MAME trace sound-command evidence:

```text
912   0xE6 credit_added
1267  0xF5 game_started
1402  0xEA player materialize / appear
1577  0xEB laser
1596  0xF9 lander hit
```

MAME state evidence shows score `150` at frame `1595`. Clean now reaches the
same first-lander hit by spawning the first wave lander at the MAME source
position, advancing sparse laser bytes at the source-backed column rate, and
delaying the laser and lander-hit sound commands to the MAME-observed frames.

Clean event timeline after the current repair:

```text
911   CreditAdded
1265  GameStarted
1402  UnmappedSoundCommand { command: 234 }
1576  UnmappedSoundCommand { command: 235 }
1595  UnmappedSoundCommand { command: 249 }
```

The event timeline is indexed by input frame, so clean sound commands at
`1576` and `1595` correspond to state frames `1577` and `1596`, matching MAME.
Clean no longer emits the earlier post-hit `0xFC` enemy-shot commands in this
tail. Reserve activation now waits for the MAME-observed first-lander restore
delay, restores one reserve lander at state frame `1800`, and avoids
immediately filling the screen with a five-lander batch.

Result: pass for the current laser-hit window. In
`target/reference-media/gameplay-laser-hit-single-center-check/report.json`,
the bounded `25.0-27.5s` full visual report is within current thresholds
(`full` RMS `29.09`, MAE `5.13`) with `playfield` RMS `10.45` and
`laser_band` RMS `15.26`. Audio passes through the stochastic-noise gate:
`noise_like_pass=true`, envelope correlation `0.716`, RMS ratio `1.206`, and
zero-crossing ratio `1.160`. The gameplay explosion now retains the destroyed
enemy's current source picture descriptor, and the first visible enemy
explosion frame is delayed until the expanded source pixels have separated into
fragments rather than rendering the full source-picture grid.
The matching live materialization/coalescence manifest entry now also accepts
`target/reference-media/gameplay-laser-hit-single-materialize-check/report.json`
as an all-axis report for the same bounded MAME-vs-clean window.

Expanded-object diagnostic trace
`target/reference-media/mame/traces/gameplay-laser-hit-single-expanded.debug.tsv`
shows the first lander hit entering slot `0x9C00` at frame `1595` with
`RSIZE=0x0100`, picture descriptor `0xF98F`, `CENTER=0x7184`, and
`TOPLFT=0x7080`. Clean now projects gameplay enemy explosion pixels from that
source-style center instead of deriving the center from the full picture width.

### Down030 Post-Death Laser Visual

Input program used:

```text
none*900;coin*4;none*360;start_one*10;none*180;down*30;none*952;fire*45;none*350
```

MAME trace evidence for this branch shows the target2 collision/death tail and
the second-life laser/appearance cadence:

```text
2178  0xF9 target2 hit
2179  0xEE player death tail
2187  0xEE player death tail
2195  0xE8 player death tail
2324  0xEC stop death tail
2344  first stock drop to one life
2439  0xEB second-life laser
2447  0xEA post-death appearance/materialize
```

Result: all-axis pass for the bounded second-life player laser/materialize
window. In
`target/reference-media/laser-hit-down030-fire2437-check-fixed/report.json`,
the report compares MAME `40000ms-42500ms` against clean `40000ms-42500ms` with
full visual RMS `36.51`, visual MAE `6.93`, playfield RMS `33.91`,
laser-band RMS `32.89`, terrain RMS `18.23`, audio envelope correlation
`0.864`, audio RMS ratio `1.008`, and zero-crossing ratio `0.971`.

This clip also verifies the previously failing default post-death restart
branch: clean now emits the second-life laser at frame `2439` and the
appearance/materialize command at frame `2447`, matching MAME. The clean
materialize waveform uses the Williams `APPEAR`/`LITEN` sweep cadence so the
laser-plus-materialize audio passes the stochastic-noise gate.

### Non-Lander Target6 Shot And Explosion

Input program used:

```text
target/reference-media/mame/non-lander-target6-fire2524/traces/non-lander-target6-fire2524-media.inputs.txt
```

MAME trace evidence in this `down029/fire2524` branch covers the target6
converted-mutant shot sequence, player/enemy collision tail, and `SCZP1`
explosion descriptor. Clean matches the trace rows for target6 shot commands
at `2872` and `2959`, collision-tail commands at `3012`, `3013`, `3021`,
`3029`, and `3158`, and the source explosion center `0x21A9`.

Result: pass for the bounded non-lander shot/explosion/materialization window.
In `target/reference-media/non-lander-target6-fire2524-check/report.json`, the
MAME stream starts at `45760ms` and compares against clean capture frames
`2750-3160`. The report passes with full visual RMS `31.72`, visual MAE
`5.74`, playfield RMS `17.46`, laser-band RMS `19.64`, and terrain RMS
`26.94`. Audio passes through the stochastic-noise gate with envelope
correlation `0.714`, RMS ratio `1.192`, and zero-crossing ratio `1.298`.

### Organic Non-Lander Long-Run Trace Inventory

Input program candidate:

```text
none*900;coin*4;none*360;start_one*10;altitude_up*5726
```

The existing MAME trace
`target/reference-media/mame/rescue-terrain-cycle/extended_hold_up_7000/traces/extended_hold_up_7000.debug.tsv`
now provides the strongest organic long-run non-lander family inventory. It
contains post-game/attract source picture rows for `BMBP1` bomb shells from
frames `2073-7000`, `BXPIC` bomb explosions from `3709-7000`, `SCZP1`
converted mutants from `2704-7000`, `UFOP1` baiters from `5811-7000`, `TIEP1`
bombers from `5993-7000`, `PRBP1` pods from `6176-7000`, and `SWPIC1` swarmers
from `6364-7000`.

The matching expected TSV records sound commands in this long-run branch for
existing accepted families: lander/baiter-style shot command `0xFC` appears at
frames `2074-3701`, mutant shot `0xF6` at `2824-2839`, bomb/projectile/aux
`0xEE` at `2536-3719`, and cannon/mutant-hit `0xE8` at `2841-3727`. It does
not yet prove organic swarmer-shot `0xF3`, pod-hit `0xFA`, bomber-hit `0xFE`,
or baiter/swarmer hit `0xF8` in the same bounded media window.

The `2026-05-29` media-capture trial generated the MAME MP4/WAV, matching
clean GIF/WAV/debug files, and
`target/reference-media/organic-nonlander-holdup-7000-check/report.json`.
Initial inspection found the clean candidate stuck in a static `GameOver`
screen, but the follow-up fix now hands the short enemy-collision post-game
residual back to the normal attract scoring cycle. The regenerated report
passes with full visual RMS `36.41`, visual MAE `7.13`, playfield RMS `40.10`,
laser-band RMS `28.75`, terrain RMS `37.20`, and an audio silence pass against
a near-silent reference/candidate window. This closes the bounded
post-game/attract non-lander scoring-presentation proof. It does not prove
organic live-gameplay non-lander hit/shot waveform breadth.

A follow-up `2026-05-29` hold-down organic media trial used input
`none*900;coin*4;none*360;start_one*10;altitude_down*5726` and compared MAME
frames `4300-4700` against the matching clean window. The MAME debug trace
contains converted-mutant rows around frame `4381`, baiter rows around frame
`4637`, and swarmer-explosion rows around frame `4655`. The generated visual
report at `target/reference-media/organic-nonlander-holddown-7000-check/report.json`
passes with `acceptance_mode=visual`, full visual RMS `28.22`, visual MAE
`4.88`, playfield RMS `7.59`, laser-band RMS `5.41`, and terrain RMS `21.39`.
Audio remains non-accepted for this clip: nearby sound commands are existing
accepted families (`0xF1`, `0xEE`, and `0xFC`), while the remaining
non-lander-specific `0xFE`, `0xFA`, `0xF8`, and `0xF3` bytes still only appear
in the isolated sound-command captures.

The repeatable `make reference-window-scan` and
`make reference-window-scan-organic` passes confirm that boundary across the
current generated MAME trace tree. With all traces included, the scan finds
`16` target sound-command hits, `149305` object evidence rows, `113279`
terrain status rows, `2` `TERBLO` process rows, and zero candidate windows
where target commands are within `24` frames of non-lander object evidence.
With synthetic/state-steered paths excluded, the organic scan covers `184`
expected traces, `147495` object evidence rows, `105596` terrain status rows,
zero `TERBLO` process rows, and zero target command hits. The JSON reports are
written to `target/reference-media/reference-window-scan.json` and
`target/reference-media/reference-window-scan-organic.json`.

### Long No-Input Gameplay Observation

Input program used:

```text
none*900;coin*4;none*360;start_one*10;none*2400
```

The first long observation attempt used the older short `start_one*4` timing and
never produced a reliable live-play trace. The corrected delayed-start capture
reaches live play and records several remaining fidelity windows in one run.

MAME trace sound-command evidence:

```text
912   0xE6 credit_added
1267  0xF5 game_started
1402  0xEA first player/enemy materialize
1851  0xEA later enemy materialize
2030  0xF9 lander hit during player collision
2031  0xEE player-death tail start
2047  0xE8 cannon/explosion tail
2176  0xEC player-death stop
2550  0xFC lander shot
2599  0xEE second player-death tail start
2616  0xE8 second cannon/explosion tail
2744  0xEC second player-death stop
2866  0xEA post-game enemy materialize sequence
2970  0xFC post-game lander shot
3585  0xEE post-game explosion/death tail
3602  0xE8 post-game cannon/explosion tail
```

MAME state evidence in this no-input run shows the first player/lander
collision scoring `150` points at the `0xF9` command window, followed by the
player-death tail at `2031`, `2039`, `2047`, and `2176`. Clean candidate media
for the same input now reproduces the first live materialize command at `1402`,
the second live materialize command at `1851`, and the first
collision/death-tail command sequence at `2030`, `2031`, `2039`, `2047`, and
`2176`. Clean no longer emits the earlier extra first-lander `0xFC` shot
command at `1914`; MAME does not emit a lander-shot command before the first
no-input collision.

Runtime fixes taken from this evidence:

- Player-enemy collision now destroys and scores the enemy first, then applies
  player damage. That matches the MAME `0xF9` lander-hit / score `150` event
  before the `0xEE` player-death tail.
- Spawned wave enemies now get a clean expanded-object appearance lifecycle
  that follows the MAME live lander `RSIZE` contraction from `0xAD00` toward
  `0x8000`. While that lifecycle is active, normal enemy sprite projection for
  the matching enemy is suppressed and the source picture renders as coalescing
  expanded-object pixels that track the moving clean enemy.
- The first wave now has a bounded early reserve cadence: the second live
  lander materializes at clean input frame `1851`, matching the MAME `0xEA`
  command timing without restoring the old five-lander reserve batch.
- The second live lander now follows the no-input MAME collision path by using
  the observed first-wave reserve spawn position, X velocity, target slot, and
  Y subpixel phase. Clean emits the first collision/death-tail command frames
  at `2030`, `2031`, `2039`, `2047`, and `2176`, matching the MAME trace.
- Early first-wave reserve activation now defers active lander shot timers so
  the original first-wave lander cannot emit the clean-only pre-collision
  `0xFC` command at `1914`.

Later repairs now align the clean sound/score timeline through the first death
tail, second post-respawn death tail, final game-over handoff, and residual
post-game materialize/shot/explosion windows. Remaining work from this clip is
visual: post-game residual object placement and any stricter crop-level
acceptance for the later materialize/shot/explosion presentation.

### High-Score / Hall Of Fame Return

Zeroed-CMOS input program used:

```text
none*900;coin*4;none*360;start_one*10;none*300;fire*30;none*3000
```

MAME trace sound-command and state evidence:

```text
912   0xE6 credit_added
1267  0xF5 game_started
1402  0xEA player materialize / appear
1577  0xEB laser
1596  0xF9 lander hit
3615  attract phase after final death flow
4033  score 350 visible in Hall of Fame table
4179  0xEC game-over/background stop command
```

Result: the red-label MAME run does not enter a player initials prompt. This
was verified with three probes: operator high-score reset, blank NVRAM, and
source CMOS defaults with every all-time high-score value seeded to zero. The
zeroed-CMOS run reached score `350`, returned through `GAME OVER`, and showed
the score in the Hall of Fame table. Clean runtime therefore follows the
MAME-backed no-entry Hall-of-Fame return after game over. The local capture
helper now has `--zero-high-scores` so this probe can be reproduced without
committing generated NVRAM or media artifacts.

### Lander Pickup And Pull

Input program used:

```text
none*900;coin*4;none*360;start_one*10;none*180;up*2600;none*300
```

MAME trace sound-command evidence through frame `3100`:

```text
912   0xE6 credit_added
1267  0xF5 game_started
1402  0xEA player materialize / appear
1851  0xEA later enemy materialize
2204  0xF4 first lander pickup
2369  0xF4 second lander pickup
2524  0xF1 first pull burst begins
2533  0xF1 first pull burst ends
2536  0xEE first conversion/loss
2693  0xF1 second pull burst begins
2702  0xF1 second pull burst ends
2705  0xEE second conversion/loss
2841  score 150 with 0xE8 explosion/cannon tail
2986  score 150 with 0xEC background stop
```

Current clean result: the same run stays in play through the MAME frame-`1851`
materialization window because coalescing enemies are not collidable until
their appearance phase completes. The clean runtime now preserves the selected
first-wave reserve target, uses the source `12px` LANDG passenger offset,
keeps the target-list subpixel fraction during grab alignment, applies the
source `GEXEC` intra-wave `WDELT` speed/shot-timer delta, advances source
lander velocity on a source-style object velocity cadence, and preserves the
captured flee `OYV` instead of refreshing it from later wave deltas. Clean
also maps source process sleeps onto clean frame counters as post-update
remaining frames, so source `PTIME` values of `6`, `4`, and `1` wake after
clean sleeps of `5`, `3`, and `0`.

Current focused acceptance now matches the MAME pickup/pull/conversion rows by
input frame: the first pull/conversion window is `2524-2536`, the second
pull/conversion window is `2693-2705`, and the first five lander shot-state
rows in the `hold-up` trace match.

Result: pass for the bounded pickup/pull media window. In
`target/reference-media/abduction-hold-up-pickup-pull-check/report.json`, the
MAME stream starts at `36675ms` and compares against the clean stream at
`899ms`, covering the first pickup through the second pull/conversion. The
report passes with full visual RMS `28.61`, visual MAE `4.99`, playfield RMS
`4.86`, laser-band RMS `3.25`, and terrain RMS `20.18`. Audio passes through
the stochastic-noise gate after extending the standalone `0xEE` human-loss /
lightning tail to match the MAME conversion window: envelope correlation
`0.613`, RMS ratio `1.066`, and zero-crossing ratio `1.076`. Remaining work is
no longer this pickup cadence.

The local MAME inventory now contains state-steered falling-human catch,
safe-landing, and terrain-blow media windows plus the organic `hold-up`
pickup/pull/conversion media window. Together those close the bounded
rescue/loss target: player catch/rescue is covered by
`afall-player-catch-check`, safe landing is covered by
`afall-safe-landing-check`, and lander-driven human loss/conversion is covered
by `abduction-hold-up-pickup-pull-check`. Organic rescue/loss and last-human
terrain-blow gameplay captures are still useful later as extra evidence, but
they are no longer active blockers. Scripted MAME media captures write
`target/reference-media/*/traces/*.debug.tsv` with `ASTCNT`, `terrain_blown`,
`PCRAM`, and `OVCNT`, while clean candidate debug TSVs include the clean
`terrain_blow` snapshot.

## Scripted Smoke Result

Command used:

```sh
INPUT='none*900;coin*4;none*120;start_one*4;none*30;fire*12;reverse*4;none*40'
REPORT_DIR=target/reference-media/scripted-fire-reverse-smoke-make-check

make reference-mame-capture \
  MAME_REFERENCE_BASENAME=scripted-fire-reverse-smoke \
  REFERENCE_INPUT_PROGRAM="$INPUT"

make reference-clean-capture \
  CLEAN_REFERENCE_BASENAME=scripted-fire-reverse-smoke \
  REFERENCE_INPUT_PROGRAM="$INPUT"

make reference-media-check \
  REFERENCE_MEDIA=target/reference-media/mame/scripted-fire-reverse-smoke.mp4 \
  REFERENCE_AUDIO=target/reference-media/mame/scripted-fire-reverse-smoke.wav \
  CANDIDATE_MEDIA=target/reference-media/clean/scripted-fire-reverse-smoke.gif \
  CANDIDATE_AUDIO=target/reference-media/clean/scripted-fire-reverse-smoke.wav \
  REFERENCE_MEDIA_REPORT_DIR="$REPORT_DIR" \
  REFERENCE_MEDIA_REPORT_ONLY=1
```

The harness produced comparable media and a report, but the selected window is
not a fair gameplay acceptance clip. At the compared gameplay-offset contact
sheet, MAME is still rendering Williams/presents attract frames while clean has
already reached gameplay. This run is therefore retained as harness evidence
only; laser, reverse, and sound fixes must use timestamp-aligned clips from the
inventory above or a later gameplay MAME capture.

## Coverage Boundaries

The accepted clips above are green, but they are not proof of every possible
arcade state. The current closure audit tracks the remaining proof boundaries:
`docs/fidelity/release-closure-audit.md`.
